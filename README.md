# Movie-Recommender-System
A content-based movie recommender system is built using Python, with data preprocessing, feature extraction via CountVectorizer and cosine similarity, integration of TMDB API for movie details, and final deployment on Heroku.
